/* eslint-disable import/prefer-default-export */

export const headerStyle = (
  title: string,
  headerShown: boolean,
): object => (
  {
    title,
    headerShown,
    headerBackTitleVisible: false,
    headerTitleStyle: {
    },
  });
